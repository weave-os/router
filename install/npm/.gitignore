# Generated by scripts/copy-installer.js (prepack hook).
# Canonical copies live in ../install.sh, ../uninstall.sh, ../cc-statusline.sh,
# ../registry.sh, ../directives.tsv, ../commands/, ../codex-skills/, and
# ../../LICENSE.
install.sh
uninstall.sh
cc-statusline.sh
registry.sh
directives.tsv
commands/
codex-skills/
pi-router/
opencode-weave/
LICENSE

# Local `npm pack` / `npm install <tarball>` artifacts. The package has no
# runtime deps, so no lockfile to track.
*.tgz
package-lock.json
node_modules/
